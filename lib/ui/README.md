# lib/ui

Browser-neutral UI utilities shared by client components.

| File | Purpose |
|------|---------|
| `tiptap-lowlight.ts` | Single registered `lowlight` instance for TipTap `CodeBlockLowlight` (syntax highlighting) |
