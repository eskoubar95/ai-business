# hooks

Shared React hooks used across client components.

| File | Purpose |
|------|---------|
| `use-outside-click.ts` | Close floating menus/popovers when clicking outside a referenced element |
