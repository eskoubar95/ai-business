# Neon — use pooled connection string for the Next.js app (serverless-friendly).
# Use a direct (non-pooled) URL only for drizzle-kit if your workflow requires it.
DATABASE_URL=

# Neon Auth — from Neon Console: Project → Branch → Auth → Configuration (Auth URL).
NEON_AUTH_BASE_URL=

# Cookie signing secret for Neon Auth session helpers (32+ characters). Example: openssl rand -base64 32
NEON_AUTH_COOKIE_SECRET=
