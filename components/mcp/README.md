# MCP UI

| Component | Role |
|-----------|------|
| `mcp-installer.tsx` | Agent edit page: install credential + grant access to that agent. |
| `mcp-library.tsx` | Settings: business-level credential list, add/remove, per-agent toggles (`grantMcpAccessToAgent` / `revokeMcpAccessFromAgent`). |
