# Webhook UI

| File | Role |
|------|------|
| `webhook-deliveries-table.tsx` | Server-friendly table of `webhook_deliveries` rows (status chips, idempotency key). Used from **Settings → Webhooks**. |

The `/dashboard/webhooks` route redirects to `/dashboard/settings?section=webhooks`.
