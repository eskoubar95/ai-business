# Task 1.1 Log — Schema Migrations + Data Migration

**Status:** Done  
**Branch:** phase2/stage1-backend  
**Date:** 2026-04-30  

## What was done

- Extended `db/schema.ts` for Phase 2: `businesses` metadata columns, `user_settings`, `agent_archetypes`, `agent_documents`, `skill_files`, `agent_mcp_access`, enums `task_status` / `task_log_author_type`, tables `tasks` / `task_logs`, MCP pivot to `business_id`, removal of `agents.instructions` and `skills.markdown` from the Drizzle model after migration.
- **`0003_harsh_turbo.sql`** generated via `npm run db:generate`, then amended with seed archetypes + data-copy SQL (soul/tools/heartbeat, SKILL.md, MCP business_id, junction inserts).
- **`0004_phase2_drop_legacy_columns.sql`** added manually (drizzle-kit errored updating `_journal.json` on first `generate` after final schema); contains MCP dedupe, column drops, `business_id` NOT NULL, unique `(business_id, mcp_name)`.
- Server code updated: `lib/agents/actions.ts` (transactions + `agent_documents`, `AgentWithInstructions`), `lib/skills/actions.ts` (`skill_files`), `lib/memory/retrieval.ts`, `lib/mcp/actions.ts` (business upsert + `agent_mcp_access`), `lib/notion/client.ts`, `components/agents/agent-form.tsx` type import.
- `db/README.md` documents Phase 2 tables and migration notes.

## Commands run

- `npm run db:generate` (initial `0003`; second run failed on journal write — resolved by hand-writing `0004` + journal entry).
- `npm run db:migrate` — success against Neon via configured URL.
- `npm test -- --run` — all green (20 tests).
- `npx tsc --noEmit` — clean.
- `npm run lint` — clean.
- `npm run build` — clean after sequential builds (first cold `.next` run occasionally misses `.next/types/*`; rerun succeeds).

## Files changed

- `db/schema.ts`, `db/README.md`
- `drizzle/0003_harsh_turbo.sql`, `drizzle/0004_phase2_drop_legacy_columns.sql`, `drizzle/meta/_journal.json`, `drizzle/meta/0003_snapshot.json`, `drizzle/meta/0004_snapshot.json`
- `lib/agents/actions.ts`, `lib/skills/actions.ts`, `lib/memory/retrieval.ts`, `lib/mcp/actions.ts`, `lib/notion/client.ts`
- `components/agents/agent-form.tsx`

## Decisions made

- **MCP duplicates:** Before enforcing unique `(business_id, mcp_name)`, migration keeps earliest credential per pair (by `created_at`, `id`), rewires `agent_mcp_access`, deletes losers — aligns with “one credential library entry per MCP name per business”.
- **UI compatibility:** Kept API surface `instructions` on agents via `getAgentsByBusiness` / `createAgent` / `updateAgent` mapped from/to `agent_documents.slug = soul`.
- **`0004` journal:** Added manually because drizzle-kit failed writing `_journal.json` on Windows in one attempt; snapshot `0004_snapshot.json` already existed from that partial run and matches final schema.

## Issues encountered

- Drizzle `generate` after removing deprecated columns failed with `UNKNOWN` writing `_journal.json` — mitigated by custom SQL + manual journal entry.
- `next build` sometimes fails type phase when `.next/types` is missing on first pass — rerunning `npm run build` fixes locally.

## Validation results

- **Quality gate:** Green — `npm test`, `npm run lint`, `npx tsc --noEmit`, `npm run db:migrate`, `npm run build` (after sequential build) all succeeded.
