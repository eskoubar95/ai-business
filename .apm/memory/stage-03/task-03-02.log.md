---
stage: 3
task: 2
title: Roster UI, org chart, MCP dashboard, teams
agent: frontend-agent
status: Success
important_findings: false
compatibility_issues: false
---

# Task 3.2 - Roster UI, org chart, MCP dashboard, teams

## Summary

Delivered dashboard routes for agents and teams, client components for markdown instructions, skills, MCP install modal, org chart, Playwright `tests/agents.spec.ts`, plus `lib/dashboard/business-scope.ts` for `businessId` resolution and nav links.

## Details

- Added `@uiw/react-md-editor` with dynamic import wrappers (`components/agents/markdown-editor-field.tsx`).
- Agents list/edit/new pages call existing server actions (`lib/agents/actions.ts`, `lib/skills/actions.ts`, `lib/mcp/actions.ts`). Agent cards show a static **idle** badge until Stage 4 orchestration (`getAgentStatus`).
- MCP installer builds payloads from `lib/mcp/config.ts` field metadata; credentials saved only via `saveMcpCredential`.
- Teams use existing `createTeam` / `addTeamMember` / `listTeamsByBusiness` / `getTeamWithMembers` (`listTeamsByBusiness` was already present on branch).
- Extended `app/components/nav-shell.tsx` with Agents and Teams links (paths without query redirect to first business server-side).

## Output

- `lib/dashboard/business-scope.ts`, `lib/dashboard/README.md`
- `app/dashboard/agents/page.tsx`, `new/page.tsx`, `[agentId]/edit/page.tsx`
- `app/dashboard/teams/page.tsx`, `new/page.tsx`, `[teamId]/page.tsx`
- `components/agents/agent-form.tsx`, `markdown-editor-field.tsx`, `skill-manager.tsx`, `org-chart.tsx`, `team-create-form.tsx`
- `components/mcp/mcp-installer.tsx`
- `tests/agents.spec.ts`
- `app/README.md` route map updates
- `package.json` / lockfile: `@uiw/react-md-editor`

## Validation

- `npm run lint`: clean.
- `npm run build`: succeeded (Next.js 15).
- `npm test -- --run`: all Vitest tests passed.
- `npm run test:e2e` for `agents.spec.ts` not executed in this environment (requires real `E2E_EMAIL`, `E2E_PASSWORD`, and DB); spec mirrors `grill-me.spec.ts` auth/onboarding pattern and skips when vars unset.

## Issues

None.
