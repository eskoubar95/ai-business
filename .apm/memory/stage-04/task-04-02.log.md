# Task 4.2 log — Skills UI + MCP library + webhook config (Frontend)

## Scope

Phase 2 Stage 4 frontend per `.apm/plan.md` Task 4.2.

## Delivered

- **Skills dashboard:** `app/dashboard/skills/page.tsx` — business scope, `listSkillsOverviewByBusiness`, agent assignment checkboxes, ZIP/folder/multi-file upload + GitHub install modals (`components/skills/*`).
- **Data:** `listSkillsOverviewByBusiness`, `listAgentSummariesByBusiness`; client ZIP helpers `lib/skills/skill-upload-files.ts` (depends on `jszip`).
- **Settings:** inbound webhook URL + delivery count; `McpLibrary` (`components/mcp/mcp-library.tsx`) with add/remove credential and per-agent toggles; `loadSettingsIntegrationsPanel` + `getPublicOrigin`.
- **Nav:** Skills link in `app/components/nav-links.tsx`; `DashboardScopedPath` includes `/dashboard/skills`.
- **Build fix:** moved `normalizeSkillFilePath` / `parseGitHubRepoUrl` to `lib/skills/skill-file-utils.ts` so `"use server"` `file-actions.ts` has only async exports (Client Components may import install actions).

## Verification

- `npm test` — green (incl. `lib/skills/__tests__/skill-upload-files.test.ts`).
- `npm run lint` — green.
- `npm run build` — green.

## Quality gate

**Green** — scoped to Task 4.2; tests + lint + build pass; no secrets.
