# Task 4.1 log — Skills file tree + Webhook + MCP library actions

**Branch:** `phase2/stage4-backend`  
**Gate:** Green — `npm test -- --run` and `npm run build` pass.

## Delivered

- `lib/skills/file-actions.ts` — `installSkillFromFiles`, `installSkillFromGitHub`, `deleteSkillFiles`, `collectGitHubSkillFiles`, path/GitHub URL helpers; requires `SKILL.md`; optional `GITHUB_TOKEN`.
- `app/api/webhooks/[businessId]/receive/route.ts` — POST, `X-Idempotency-Key`, `X-Webhook-Signature` (HMAC), idempotent `webhook_deliveries`, `orchestration_events` type `webhook_trigger`, 202.
- `lib/mcp/actions.ts` — `saveMcpCredential(businessId, …)` returns `{ id }`, `grantMcpAccessToAgent`, `revokeMcpAccessFromAgent`, `getMcpCredentialsByBusiness`, `getMcpCredentialsForAgent`, `deleteMcpCredential` (business-scoped auth).
- UI: `McpInstaller` + Notion panel call save + grant; dashboard pages use `getMcpCredentialsForAgent`.
- Tests: `lib/skills/__tests__/file-actions.test.ts`, `lib/mcp/__tests__/actions-mcp.test.ts`, `app/api/webhooks/.../route.test.ts`.
- Docs: `lib/skills/README.md`, `lib/mcp/README.md`, `lib/webhooks/README.md`; `.env.example` — `GITHUB_TOKEN`.

## Manual smoke

- POST webhook with valid HMAC + idempotency key → 202; repeat key → 202 without second orchestration row.
- Install MCP from agent edit → credential row + `agent_mcp_access` for that agent.
