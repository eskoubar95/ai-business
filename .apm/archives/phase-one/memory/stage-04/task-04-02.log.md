---
stage: 4
task: 2
title: Approval Dashboard & Notion UI
agent: frontend-agent
status: Success
important_findings: true
compatibility_issues: false
---

# Task 4.2 - Approval Dashboard & Notion UI

## Summary

Delivered dashboard pages for approvals, Notion connection/sync, and webhook deliveries; wired agent roster cards to live `getAgentStatus` with Suspense; global nav shows Approvals with pending badge plus Notion/Webhooks links; added guarded E2E seed route and `tests/approvals.spec.ts`.

## Details

- Extended `DashboardScopedPath` and `resolveBusinessIdParam` for approvals/notion/webhooks routes.
- Added server-side reads: `lib/approvals/queries.ts`, `lib/webhooks/deliveries-queries.ts`, `lib/orchestration/notion-sync-queries.ts`; `lib/notion/dashboard-actions.ts` for sync invocation from UI.
- Built UI components under `components/approvals`, `components/notion`, `components/agents` (`agent-card`, async `agent-status-badge`).
- Converted root `NavShell` to async server component for pending approval count via `countPendingApprovalsForUser`.
- Root `app/layout.tsx` is async to render async `NavShell`.
- Playwright seed: `POST /api/e2e/seed-approval` (non-production, `x-e2e-secret` + session); `.env.example` documents `E2E_SETUP_SECRET`.

## Output

- `app/dashboard/approvals/page.tsx`, `app/dashboard/approvals/[approvalId]/page.tsx`
- `app/dashboard/notion/page.tsx`, `app/dashboard/webhooks/page.tsx`
- `app/dashboard/README.md`
- `components/approvals/approval-card.tsx`, `components/notion/notion-connection-panel.tsx`, `components/notion/notion-sync-table.tsx`
- `components/agents/agent-card.tsx`, `agent-status-badge.tsx`, `README.md`
- `app/components/nav-shell.tsx`, `app/layout.tsx`
- `app/dashboard/agents/page.tsx` (uses `AgentCard`)
- `lib/approvals/queries.ts`, `lib/webhooks/deliveries-queries.ts`, `lib/orchestration/notion-sync-queries.ts`, `lib/notion/dashboard-actions.ts`
- `app/api/e2e/seed-approval/route.ts`
- `tests/approvals.spec.ts`
- README updates under `lib/dashboard`, `lib/approvals`, `lib/webhooks`, `lib/orchestration`, `lib/notion`

## Validation

- `npm run lint` — pass
- `npm test -- --run` — pass (20 tests)
- `npm run build` — pass
- `npx playwright test tests/approvals.spec.ts` — **did not complete**: webServer health check timed out because port 3000 was already in use (Next.js bound to 3001 while Playwright expects `baseURL` 3000). Re-run with port 3000 free or adjust dev URL.

## Issues

- Local Playwright run blocked by port mismatch/timeouts above; CI/local verification should confirm full E2E with `E2E_EMAIL`, `E2E_PASSWORD`, `E2E_SETUP_SECRET`, and exclusive port 3000 for `npm run dev:e2e`.

## Important Findings

- Async `NavShell` triggers `[auth.getSession]` dynamic rendering notices during `next build` for statically analyzed routes; build still succeeds.
- Playwright `webServer.url` is fixed to `localhost:3000`; concurrent processes on that port prevent automated E2E startup unless reuseExistingServer hits an app already on 3000.
